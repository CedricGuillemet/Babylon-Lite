# MeshLoD Task Board

## Queue

| M | ID | Task | Skill | Notes |
|---|---|---|---|---|
|  | T-35 | Execute implementation-plan/08_02_demo_controls_network_camera.md | execute-implementation-plan |  |
|  | T-36 | Execute implementation-plan/08_03_demo_diagnostics_debug.md | execute-implementation-plan |  |
|  | T-37 | Execute implementation-plan/08_04_verify_demo_workflows.md | execute-implementation-plan |  |
|  | T-38 | Execute implementation-plan/09_01_full_validation_traceability.md | execute-implementation-plan |  |

## Completed

| M | ID | Task | Skill | Notes |
|---|---|---|---|---|
|  | T-01 | Goals - review or create goals.md | review-goals | done 2026-07-22 - goals.md |
|  | T-02 | Visual mocks (optional) | create-html-mock | done 2026-07-22 - skipped, use existing demo styling |
|  | T-03 | Requirements | write-requirements | done 2026-07-22 - requirements.md |
|  | T-04 | Architecture | write-architecture | done 2026-07-22 - architecture.md |
|  | T-05 | Implementation plan | write-implementation-plan | done 2026-07-22 - implementation-plan/ (33 tasks) |
|  | T-06 | Execute implementation-plan/01_01_scaffold_converter.md | execute-implementation-plan | done 2026-07-22 - configure+build+CTest 6/6; meshoptimizer+cgltf pinned to exact SHAs |
|  | T-07 | Execute implementation-plan/01_02_cli_and_primitive_selection.md | execute-implementation-plan | done 2026-07-22 - full option parse/validation + deterministic primitive naming; CTest 6/6 |
|  | T-08 | Execute implementation-plan/01_03_normalize_gltf_geometry.md | execute-implementation-plan | done 2026-07-22 - cgltf ingest/validate/select + normalize (indices, angle-weighted normals, bounds); 15 fixtures; CTest 6/6 |
|  | T-09 | Execute implementation-plan/01_04_build_cluster_hierarchy.md | execute-implementation-plan | done 2026-07-22 - clusterlod DAG + 8-wide forest; exact terminal coverage + reachability validation; grid fixture 6 groups/5 levels; CTest 6/6 |
|  | T-10 | Execute implementation-plan/02_01_define_binary_contract.md | execute-implementation-plan | done 2026-07-22 - mlod_format.h layout constants + LE/checked helpers; CRC32C + SHA-256 with published vectors; source/build fingerprints; format_vectors test; CTest 7/7 |
|  | T-11 | Execute implementation-plan/02_02_pack_deterministic_pages.md | execute-implementation-plan | done 2026-07-22 - deterministic 64-256KiB page packing (meshopt vertex/index streams, oct-normal+half UV), pinned prefix, decode round-trip + byte-identical repack; CTest 7/7 |
|  | T-12 | Execute implementation-plan/02_03_write_validate_publish.md | execute-implementation-plan | done 2026-07-22 - full container writer (header/dir/sections/pages + CRCs), independent validator, canonical stats, atomic multi-output publish; end-to-end grid->.mlod validates; CTest 7/7 |
|  | T-13 | Execute implementation-plan/02_04_converter_fixtures_and_statue.md | execute-implementation-plan | done 2026-07-22 - statue converted to 3 .mlod (98366/119201/93519 src tris, 12 levels, 40/46/38 pages, exact terminal coverage); byte-identical repeat; --validate-only passes; assets+stats committed; CTest 7/7 |
|  | T-14 | Execute implementation-plan/03_01_public_api_and_errors.md | execute-implementation-plan | done 2026-07-22 - c9016474; facade+errors+runtime types, option validation, no-side-effect lazy imports; lint(eslint+tsc)/26 unit tests green |
|  | T-15 | Execute implementation-plan/03_02_parse_mlod_buffers.md | execute-implementation-plan | done 2026-07-22 - d0816a75; full LE parser/validator mirroring C++ validateContainer, CRC32C, record interfaces; 25 tests incl. 3 real statue assets vs statue-stats.json + 20-case mutation matrix; lint green |
|  | T-16 | Execute implementation-plan/03_03_range_source_bootstrap.md | execute-implementation-plan | done 2026-07-22 - 62b29c8f; range source (URL/ArrayBuffer/Blob) + strict HTTP protocol validation, coarse-first two-request bootstrap, available-aware parse; loadMeshLoD real; +lite-integration vitest project; 71 mesh-lod tests green; lint green |
|  | T-17 | Execute implementation-plan/03_04_cpu_selection_oracle.md | execute-implementation-plan | done 2026-07-22 - 55e78b91; float32 CPU oracle (traversal, culling, hysteresis, crack-free DAG cut + residency fallback, page demand); ./mesh-lod/testing subpath; committed selection fixtures; 79 mesh-lod tests green; lint green. Phase 3 complete. |
|  | T-18 | Execute implementation-plan/04_01_lazy_page_decoder.md | execute-implementation-plan | done 2026-07-22 - 04441b02; export shared MeshoptDecoderModule (type-only), mesh-lod-page-decoder with validate-before-codec + ATTRIBUTES/TRIANGLES NONE decode + index-bounds; 6 mock-decoder tests; existing decode-error unchanged; lint green |
|  | T-19 | Execute implementation-plan/04_02_load_pinned_pages.md | execute-implementation-plan | done 2026-07-22 - da04a99c; loadMeshLoD resolves after arena alloc + pinned decode/upload (fine pages unrequested); mesh-lod-cache 64 KiB block first-fit allocator + pinned accounting + device-limit clamp; MLOD_BUDGET_TOO_SMALL/MLOD_DEVICE_LIMIT; per-page residency retains decoded indices for CPU expansion; decoder-injection seam + mock device/decoder; 17 new + updated loader/options tests; lint green |
|  | T-20 | Execute implementation-plan/04_03_scene_registry_cpu_selection.md | execute-implementation-plan | done 2026-07-22 - bc8c9e00; mesh-lod-scene registry batches by exact asset+material identity, idempotent add/remove, one deferred builder/registry, scene-only _meshLoDRegistry (type-only), one-way ownership; validateSupportedPbrSubset -> MLOD_UNSUPPORTED_MATERIAL; selectMeshLoDBatch runs CPU oracle per visible instance (culling off) with hysteresis + pinned residency; runtime delegation; PBR renderable scaffold; 11 registry tests; lint green |
|  | T-21 | Execute implementation-plan/04_04_pbr_coarse_indirect_rendering.md | execute-implementation-plan | done 2026-07-22 - e5d8f96b; pbr-mesh-lod-compose (storage-fetch vertex: oct-normal/half-UV unpack + instance world/cofactor-normal; PBR/unlit fragment: SH IBL + scene lights UBO) + pbr-mesh-lod-renderable (fixed group-1 BGL, 1x1 fallbacks, per-frame CPU expand, one pass.drawIndirect/batch, no vertex/index buffers); generic renderer sees an ordinary opaque Renderable. Browser-verified in real WebGPU: statue coarse 46-tri terminal LOD renders unlit + PBR-lit via 1 indirect draw, 1 resident pinned page. GPU-mock render extension + coarse-render integration test (5). lint green |
|  | T-22 | Execute implementation-plan/04_05_verify_lazy_coarse_path.md | execute-implementation-plan | done 2026-07-22 - 05fc5ebd; mesh-lod-coarse-path (public facade end-to-end + coarse-only bootstrap over a fine-failing range server: no fine fetch, all pinned resident, coarse renders across frames with no holes/failure), mesh-lod-tree-shaking build test (unrelated import retains zero MeshLoD; loadMeshLoD keeps runtime/renderable/decoder/WGSL behind dynamic chunks), bundle-size.spec MeshLoD-absence check for every scene. Mandatory pnpm test run in a clean linked worktree: build:bundle-scenes OK (zero manifest change vs committed except unrelated scene113/114/115), bundle-size parity 213/213 pass, scene parity 217 pass; the only 4 fails (scene104/105/106 physics nondeterminism, scene265 GPU precision) fail identically on baseline c2aee222 -> pre-existing, not a Phase-4 regression. Ceilings/goldens unchanged. Phase 4 complete. |
|  | T-23 | Execute implementation-plan/05_01_gpu_metadata_buffers.md | execute-implementation-plan | done 2026-07-23 - ec6c3a5b; shared float32 selection math (mesh-lod-selection-math, oracle refactored onto it, 11 oracle fixtures unchanged); mesh-lod-selection-gpu exact packed layouts (nodes 8w/groups 16w/clusters 16w/page-state 8w/instance 128B cofactor-normal+maxScale+visibility+id), device-limit-checked per-asset immutable+page-state buffers (shared), residency-signature-gated re-upload, per-batch instance/prior-state buffers with version-gated uploads + make-before-break growth via retireGpuResources; runtime.gpuSelection field + facade disposal; gpu-mock recording encoder/compute stubs; 14 mesh-lod-gpu-layout tests; 135 mesh-lod tests green; lint(eslint+tsc) green |
|  | T-24 | Execute implementation-plan/05_02_gpu_selection_compute.md | execute-implementation-plan | done 2026-07-23 - 8c9c6893; mesh-lod-selection.wgsl (reset via clearBuffer, ordered traverse/evaluateGroups/selectClusters/computeDemand at wg64; per-leaf frustum visibility = root-down traversal under conservative bounds; hysteresis asymmetric equality + prior persist; complete-residency crack-free cut; atomic-append + bounded overflow; benefit/cost page demand; 4 tables -> 1 meta buffer + groupState u32/(group,instance) to fit 8 storage buffers); runMeshLoDGpuSelection exact TS mirror over packed buffers (shared float32 math); device-keyed pipelines + explicit shared bind-group layout; per-batch groupState/selected/control buffers (make-before-break) + params UBO; MeshLoDUpdateBatch extends DrawUpdateBatch (one compute pass/target before render); extractFrustumPlanes shared; asset buffers consolidated to 1 meta + page-state; 14 model==oracle + 2 orchestration tests; 151 mesh-lod tests green; full lint(eslint+6 tsc) green. Real WGSL browser-validated in T-25/T-26. |
|  | T-25 | Execute implementation-plan/05_03_gpu_expansion_indirect_draw.md | execute-implementation-plan | done 2026-07-23 - 09eaeb58; WGSL clampSelectedCount+expandClusters (1 wg/selected cluster, lane-0 atomic reserve tri*3, decode packed u16 arena indices -> absolute vertex-word offsets + 16B draw-vertex records)+finalizeDraw; selection & expansion split into TWO compute passes (a buffer can't be writable-storage + indirect source in one sync scope); browser-caught & fixed WGSL bugs: 'meta'/'active' reserved keywords, workgroupBarrier uniform-control-flow; expansion pipelines/bind-group (no control) + make-before-break draw-vertex/draw-args buffers + dispatchWorkgroupsIndirect; runMeshLoDGpuExpansion TS mirror; pbr-mesh-lod-renderable branches selectionMode (GPU queues into MeshLoDUpdateBatch via _updateBatches, binds GPU draw buffers, one drawIndirect; CPU path unchanged); coarse-render/coarse-path pinned to CPU mode. REAL WEBGPU BROWSER VALIDATION: GPU render pixel-identical to CPU reference (MAD 0.0, 0 diff pixels) on statue coarse LOD (46 tris, 312 groups/2491 clusters/363 nodes/12 levels), 1 indirect draw, no GPU validation errors. mesh-lod-indirect-render tests (exact expansion records, overflow, 2-pass structure, growth, zero-selection, variants); 160 mesh-lod tests green; full lint green. |
|  | T-26 | Execute implementation-plan/05_04_cpu_gpu_equivalence.md | execute-implementation-plan | done 2026-07-23 - 5c863632; mesh-lod-selection-equivalence (new selection-jitter.json threaded through both selectors with no oscillation + model==oracle each step; two transformed instances independent in one batch; frustum tangent visible vs just-outside culled; incomplete-residency page-demand order+priority) + mesh-lod-render-equivalence (GPU model == CPU oracle over the REAL statue hierarchy, selected clusters expand to the CPU render's exact triangle count; exactly one drawIndirect per asset+material+target key, batch-scaled not meshlet-scaled); _normalizeMeshLoDSelectedClusterIds @internal readback hook + GPU-model re-exports on the internal testing path (off the trimmed public d.ts). 169 mesh-lod tests green; full lint(eslint+6 tsc) green. MANDATORY pnpm test in a clean linked worktree (LAB_TEST_PORT=5185): build:bundle-scenes built all 216 scenes; only scene113/114/115 manifests differ vs committed (pre-existing stale manifests, regenerate identically, unrelated to MeshLoD) -> NO MeshLoD bundle leak into any scene; parity 432 passed / 4 failed, the 4 being scene104/105/106 (physics character-controller nondeterminism) + scene265 (GPU precision) which fail identically on the Phase-4 baseline -> pre-existing, NOT a Phase-5 regression. Goldens & MAD ceilings unchanged (worktree discarded). Phase 5 complete. |
|  | T-27 | Execute implementation-plan/06_01_page_request_scheduler.md | execute-implementation-plan | done 2026-07-23 - facf7d07; mesh-lod-scheduler (bounded/dedup/cancellable/priority-ordered/retry-bounded fine-page requests per arch 11.2): per-frame demand snapshot sorted desc priority then asc id, one request/page, mutable maxConcurrentRequests, two-frame obsolete queued-removal + in-flight abort, stale generation/token discard before residency mutation, 250/1000ms bounded retries with network/408/429/5xx retryable vs abort/other-4xx/protocol/integrity/version permanent, pause suppresses only new requests/retries; injected timers+fetch for deterministic fixtures; decode/cache/upload stay runtime-owned via callbacks. Extended MeshLoDPageState to full 11.3 machine + forward-declared runtime.scheduler. mesh-lod-scheduler.test.ts 25 cases; 138 mesh-lod unit tests + full lint(eslint+6 tsc) green. |
|  | T-28 | Execute implementation-plan/06_02_bounded_page_caches.md | execute-implementation-plan | done 2026-07-23 - 8c603f64; mesh-lod-cache bounded GPU residency eviction (arch 11.4) + CPU encoded-page cache (11.5) over the immutable arena/pinned prefix (no reallocation). reserveMeshLoDArenaRun reserves a fine page's contiguous decoded run within the mutable effective budget: evicts eligible victims to satisfy budget, then evicts more to defragment a contiguous run, else returns null (allocating nothing) when only protected pages remain. isEvictable excludes pinned, non-gpu-resident (fetching/decoding/uploading), current-frame-referenced (frameRefCount>0) and <120-frame-held pages; victim order = oldest-use, then lower priority, then higher id; evictMeshLoDPage frees the run, resets page to unrequested, records id for GPU page-state sync. MeshLoDCpuPageCache always retains pinned encoded pages (counted to budget) and LRU-bounds unpinned bytes; put/get/touch/setBudget keep usedBytes exact. Runtime: MeshLoDPageRuntime gains lastUsedFrame/priority/frameRefCount; asset runtime owns cpuPageCache seeded with pinned coarse pages at load; diagnostics.cpuPageCacheUsedBytes reports retained bytes. mesh-lod-cache.test.ts 11 cases (budget boundary, victim ordering, fragmentation, pinned/current-frame/in-flight/young exclusion, 120-frame hold, CPU LRU + pinned retention + shrink); selection-gpu/gpu-layout fixtures updated; 205 mesh-lod tests + full lint(eslint+6 tsc) green. |
|  | T-29 | Execute implementation-plan/06_03_connect_streaming_residency.md | execute-implementation-plan | done 2026-07-23 - 6eaa4802; streaming residency engine wiring the per-frame demand -> scheduler -> decode -> reserve/evict -> upload -> residency-commit loop behind CPU selection (arch 11). runtime.stepMeshLoDStreaming advances the frame, applies concurrency/CPU-budget changes, holds frame references on rendered pages (released via retireGpuResources after submission, 14.1), submits generation-stamped demand, trims residency to the effective GPU budget, refreshes diagnostics; commitStreamedPage validates+decodes+caches encoded bytes+reserves an arena run (evicting eligible victims)+uploads+retains local indices+marks gpu-resident; failStreamedPage records page-local terminal failures; fetch short-circuits an evicted page's retained encoded bytes; scheduler disposed on asset disposal; @internal _schedulerTimers seam for deterministic fixtures. cache.evictMeshLoDToBudget trims committed residency to a lowered budget. scene.driveMeshLoDStreaming unions per-instance desired pages (max priority) + selected-cluster frame refs + visible/fallback/error diagnostics. renderable CPU path drives streaming and grows the draw-vertex buffer make-before-break so streamed refinement renders past the coarse bound (renderedTriangleCount>46). mesh-lod.ts setMeshLoDCacheBudget/StreamingPaused update live diagnostics. Groups refine atomically; coarse fallback survives every fine failure. mesh-lod-streaming.test.ts 8 cases (progressive residency+refinement, whole-coarse-every-frame, pause/resume, permanent-failure page-local, budget-reduction eviction, stale-generation discard, no-longer-demanded abort, disposal cancellation); updated the Phase-4 coarse-path render assertion (rendering now requests fine pages the server makes unavailable and the coarse fallback still renders whole, REQ-SEL-7). 216 mesh-lod tests + tree-shaking + full lint green. Browser/WebGPU/network + mandatory pnpm test validated with T-30. |
|  | T-30 | Execute implementation-plan/06_04_streaming_cache_fixtures.md | execute-implementation-plan | done 2026-07-23 - fad1dfed (fixtures) + 39788539 (browser-found render-bundle fix); deterministic streaming/cache verification fixtures (REQ-VERIFY-4). fake-frame-clock (injected MeshLoDSchedulerTimers manual retry clock) + fake-range-server (instrumented statue transport: retry-status sequences, held/abortable transfers, 200 full-body fallback, malformed 206, CRC corruption; records starts/completions/aborts + peak concurrency). mesh-lod-streaming-cache.test.ts 18 cases (bounded concurrency + dedup, retry-then-commit + retry-exhaustion->terminal via the clock, protocol/integrity permanent terminals, 200 fallback served from memory, abort-once-undemanded with no stale resurrection, committed-within-budget + pinned-never-evicted, and an executable REQ-VERIFY-4 clause->test inventory); +3 evictMeshLoDToBudget cache cases. 240 mesh-lod tests (unit+integration+build+no-webgpu) + full lint green. REAL-WEBGPU BROWSER VALIDATION (localhost:5174, CPU selection over real HTTP range + real meshopt WASM): statue streamed to 11 resident pages / 1 MB downloaded, refined 46 -> 14720 triangles, 0 terminal failures, 0 WebGPU validation errors after the render-bundle fix. MANDATORY pnpm test in a clean linked worktree (LAB_TEST_PORT=5197): build:bundle-scenes built all 216 scenes; parity 432 passed / 4 failed = scene104/105/106 (physics character-controller nondeterminism) + scene265 (GPU precision), identical to the documented Phase-4/5 baseline -> pre-existing, NOT a Phase-6 regression (MeshLoD tree-shaken from every scene). Only scene113/114/115 manifests drift (pre-existing stale, unrelated); goldens/ceilings unchanged (worktree discarded). Phase 6 complete. |
|  | T-39 | Reconcile early mesh-lod-runtime scaffold | execute-implementation-plan | deferred - covered by queued T-20 scene registry |
|  | T-40 | Add MeshLoD testing export subpath | execute-implementation-plan | done 2026-07-22 - completed within T-17 |
|  | T-41 | Preserve TypeScript 6 compatibility fixes | execute-implementation-plan | done 2026-07-22 - completed within T-14; no further work |
|  | T-42 | Implement GPU demand readback and adaptive draw growth | execute-implementation-plan | done 2026-07-23 - 83962086; GPU selection-mode async page-demand + diagnostics readback (control buffer -> MAP_READ staging ring copied after the compute passes, mapped once the frame submits, arch 12.3 step 8) drives the shared runtime streaming engine, plus adaptive make-before-break draw-vertex growth from the GPU-reported selected triangle count so GPU mode streams + renders refined geometry past the coarse bound. Pure decodeMeshLoDGpuReadback (benefit/stored-bytes priority per 11.1) + applyMeshLoDGpuReadback seam; readback ring/growth reset on device change + disposal; CPU path unchanged. New decode unit test + mock-device integration test (control->staging copy wiring, decode->demand->streaming->growth, stale-generation drop); 162 mesh-lod unit + 90 integration + tree-shaking + no-webgpu + full lint green. REAL-WEBGPU BROWSER VALIDATION (localhost:5174, GPU selection over real HTTP range + meshopt WASM): statue refined 46 -> 19886 triangles across 18 resident pages / 1.2 MB streamed, 0 GPU validation errors, byte-for-byte matching CPU-mode output (19886 tris / 301 meshlets). MANDATORY pnpm test in a clean linked worktree (LAB_TEST_PORT=5191): build:bundle-scenes built all 216 scenes; bundle-size 213/213 pass; parity 432 passed / 4 failed = scene104/105/106 (physics nondeterminism) + scene265 (GPU precision), identical to the Phase-4/5/6 baseline -> pre-existing, NOT a regression (MeshLoD tree-shaken from every scene; only pre-existing scene113/114/115 manifests drift). Ceilings/goldens unchanged (worktree discarded). |
|  | T-31 | Execute implementation-plan/07_01_disposal_and_scene_pruning.md | execute-implementation-plan | done 2026-07-23 - a3786b5a; frame-safe, idempotent, generation-safe, shared-asset-aware disposal (arch 14.2; REQ-LOAD-3/INT-4/INT-7/RENDER-4). disposeMeshLoDAsset marks disposed + bumps generation + aborts asset/page controllers, then retires the geometry arena + shared selection buffers via retireGpuResources (destroyed only after the next submitted frame drains) and releases retained CPU page bytes + decoded indices immediately (pages marked disposed). Renderable is non-drawable the instant its asset is disposed (updatePacket clears the binding + re-records the cached bundle once via invalidateRenderBundles; draw hard-guards on disposed); an empty CPU batch is now non-drawable too, matching the GPU path + arch 13.4. commit/failStreamedPage reject completions once disposed so a fetch racing disposal never makes a page resident. mesh-lod-disposal (5) + mesh-lod-shared-lifecycle (3) tests; 162 mesh-lod unit + 89 integration + tree-shaking + no-webgpu + full lint green. Mandatory pnpm test run at the lifecycle-milestone boundary (T-33). |
|  | T-32 | Execute implementation-plan/07_02_deferred_device_recovery.md | execute-implementation-plan | done 2026-07-23 - c636f18c; generic scene-owned recoverable-deferred-renderable seam restoring MeshLoD coarse rendering after device loss with NO feature branch in the recovery module (arch 14.3; REQ-LOAD-4/RENDER-4/INT-2). scene-core DeferredSceneGpuRecoverable{rebuild,dispose} + lazy scene._deferredGpuRecoverables + addDeferredSceneGpuRecoverable; device-lost-recovery rebuilds ordinary groups then invokes each recoverable + appends renderables before sort/frame-graph build. runtime._recoverMeshLoDAsset recreates the arena on the recovered device, re-decodes+uploads retained pinned coarse pages, resets fine pages (opportunistic streaming restore), drops the shared selection buffers (lazy rebuild), idempotent per device via runtime.gpuDevice; runtime.gpu now replaceable; pinned failure -> terminal MLOD_DEVICE_RECOVERY (no partial coarse). mesh-lod-scene registers one recoverable that recovers every registered asset (failed -> MLOD_DEVICE_RECOVERY + skipped batch) then rebuilds packets; initial build reuses the same path. mesh-lod-device-recovery (4) tests; existing device-lost-geometry-sharing green; 162 mesh-lod unit + 93 integration + tree-shaking + no-webgpu + full lint green. Mandatory pnpm test run at the lifecycle-milestone boundary (T-33). |
|  | T-33 | Execute implementation-plan/07_03_lifecycle_recovery_fixtures.md | execute-implementation-plan | done 2026-07-23 - 5b40e6e9; frame-safe lifecycle + recovery verification fixtures (arch 14; REQ-LOAD-3/LOAD-4/RENDER-4). mesh-lod-frame-refs (unit, 3): the section-14.1 frame-reference lifecycle — stepMeshLoDStreaming (CPU) and applyMeshLoDGpuReadback (GPU) hold a frameRefCount on referenced/resident pages and schedule a retirement that decrements only when the submitted frame's fence drains; queue submission is not completed work; refs accumulate per in-flight frame. mesh-lod-lifecycle (integration, 2): the CPU draw-vertex buffer grows make-before-break (old buffer destroyed only after the fence, never at growth time) and a current-frame-referenced page is never evicted under a tightened budget until unreferenced + drained. Disposal-during-async/repeated/shared and coarse-first recovery+MLOD_DEVICE_RECOVERY are covered by T-31/T-32 fixtures; existing gpu-resource-retirement + device-lost-geometry-sharing (generic seam) stay green. 165 mesh-lod unit + 95 integration + tree-shaking + no-webgpu + full lint green. MANDATORY pnpm test (lifecycle milestone T-31..T-33) in a clean linked worktree (LAB_TEST_PORT=5193): build:bundle-scenes built all 216 scenes; bundle-size 213/213 pass; parity 432 passed / 4 failed = scene104/105/106 (physics nondeterminism) + scene265 (GPU precision), identical to the T-42/Phase-4/5/6 baseline -> pre-existing, NOT a regression. scene164-device-lost-recovery (ordinary device-loss, real browser) PASSED -> the generic recoverable seam does not regress core recovery. Only pre-existing scene113/114/115 manifests drift; ceilings/goldens unchanged (worktree discarded). |
|  | T-34 | Execute implementation-plan/08_01_demo_shell_and_assets.md | execute-implementation-plan | done 2026-07-23 - f72c3c3e; standalone production-bundled MeshLoD statue demo via the public lazy path (loadMeshLoD -> createMeshLoDInstance -> addMeshLoDToScene), no demo-only loader/selector/cache/renderer (REQ-DEMO-1..3, REQ-NAME-3). demo-mesh-lod.html (fullscreen WebGPU page + shared loading overlay/progress + data-error banner + accessible control/diagnostic/legend mount points + CC-BY credit) and src/demos/mesh-lod.ts (loads repo-root source GLB only for its 3 PBR materials/node transforms, loads 3 .mlod + environment, creates 3 instances parented to the never-registered source meshes so only MeshLoD renders, orbit/zoom arc-rotate camera framed from aggregate world bounds, data-ready/data-error). .mlod stream over HTTP Range from /mesh-lod/ (sirv 206) in dev + relative to the bundle in standalone; MeshLoD fetch bypasses the loading-progress wrapper. demos-config.json mesh-lod entry; bundle-demos-core.ts copies lab/public/mesh-lod + the root GLB into the flat demo output; 1280x720 JPG thumbnail. REAL-WEBGPU BROWSER VALIDATION (localhost:5174, headless Chrome): data-ready, 311,086 src tris / 3 instances, 93 .mlod requests ALL 206 Partial Content (7.3 MB streamed), orbit+zoom work, only favicon 404 (benign). Lab typecheck green; demos need no parity/pnpm test (no engine src change). |

## Untriaged

<!-- END OF BOARD -->
